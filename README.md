# Money Changer PWA

Offline-first iPhone-friendly record encoder built with Next.js, TypeScript, Tailwind, shadcn-style components, Dexie, and IndexedDB.

## Run

```bash
npm install
npm run dev
```

Build for PWA service worker generation:

```bash
npm run build
npm run start
```
